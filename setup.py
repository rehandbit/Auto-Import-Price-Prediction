from setuptools import find_packages, setup

setup(
    name='1985 Auto Imports Database',
    version = '0.0.1',
    author = 'Rehan',
    author_email='rehan.farazmdp@gmail.com',
    packages= find_packages(),
    # install_requires=get_requirements('requirements.txt')
)